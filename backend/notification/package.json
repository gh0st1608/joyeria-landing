{
  "name": "notification",
  "version": "0.0.1",
  "description": "",
  "author": "",
  "private": true,
  "license": "UNLICENSED",
  "scripts": {
    "build": "nest build -p ./tsconfig.build.json",
    "build:function": "nest build -p ./tsconfig.build.json && cp ./dist/function.js ./dist/index.js && cp package.json ./dist/ && cp tsconfig.json ./dist/ && cp tsconfig.build.json ./dist/ && cp nest-cli.json ./dist/ && cp .env.yaml ./dist/"
  }
}
